<!-- res/drawable/edittext_underline.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true">
        <layer-list>
            <item android:top="@dimen/_29sdp"> <!-- Adjust this value to control underline position -->
                <shape android:shape="rectangle">
                    <size android:height="1dp" /> <!-- Set height to 1dp for underline effect -->
                    <solid android:color="@color/black" /> <!-- Color when focused -->
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Default/Unfocused state -->
    <item>
        <layer-list>
            <item android:top="@dimen/_29sdp"> <!-- Adjust this value to control underline position -->
                <shape android:shape="rectangle">
                    <size android:height="1dp" />
                    <solid android:color="@color/black" /> <!-- Color when not focused -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
